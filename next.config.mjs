/** @type {import('next').NextConfig} */
const nextConfig = {
    fonts: {
    google: [
      {
        family: "Courier Prime",
        variants: ["400", "700"], // Specify the weights you need
      },
    ],
  },
};

export default nextConfig;
